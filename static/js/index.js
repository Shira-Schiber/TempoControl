// Legacy carousel and slider initialization
// Note: Main functionality has been moved to main.js
window.HELP_IMPROVE_VIDEOJS = false;

$(document).ready(function() {
    // Legacy initialization (kept for compatibility)
    // Main carousel logic is now in main.js
});
